# Architectural decisions

One file per decision, `NNNN-slug.md` — the ADR's number zero-padded to four digits, so ADR-005 is
`0005-public-spis-in-the-core.md`. These record *why* the library is shaped the way it is — the
context at the time, the decision, and what it rules out. What the architecture looks like *now*
is [`DESIGN.md`](../DESIGN.md); how to use the library is [`README.md`](../../README.md).

| ADR | Decision | Status | What it rules out |
|---|---|---|---|
| [001](0001-java-21-baseline.md) | Java 21 baseline | Accepted | API or bytecode requiring a newer runtime |
| [002](0002-zero-dependency-core.md) | Zero-dependency core | Accepted | Any runtime implementation dependency in `push2u-core`; JSpecify (annotations only) is the single exception |
| [003](0003-concrete-hkdf-implementation.md) | Concrete HKDF implementation | Accepted | HKDF, the encryptor or the origin serialization as an extension point |
| [004](0004-stateless-library.md) | Stateless library | Accepted; one clause superseded by [019](0019-vapid-token-reused-until-it-nears-expiry.md) | Subscription storage, deletion or lifecycle inside the library |
| [005](0005-public-spis-in-the-core.md) | Public SPIs in the core | Accepted | A seam without an articulable difference the library cannot decide for the deployment; one transport seam serving both trust domains |
| [006](0006-aes128gcm-only.md) | `aes128gcm` only | Accepted | Legacy `aesgcm` support; a content-coding switch |
| [007](0007-expired-subscription-is-a-result.md) | Expired subscription is a result | Accepted; one clause superseded by [021](0021-retry-belongs-to-the-caller.md) | `404`/`410` as exception-driven control flow |
| [008](0008-apache-license-2-0.md) | Apache License 2.0, declared per file with an SPDX header | Accepted | A Java file without the header; a copyright year advanced after the file was created; a `NOTICE` file |
| [009](0009-standalone-repository.md) | Standalone repository | Accepted | Application-specific dependencies or configuration |
| [010](0010-pluggable-vapid-key-custody.md) | Pluggable VAPID key custody | Accepted | Hardwiring local signing into the send pipeline |
| [011](0011-size-limit-expressed-on-the-encrypted-body.md) | Size limit expressed on the encrypted body | Accepted; one clause superseded by [023](0023-one-size-limit-answerable-before-a-send.md) | A plaintext maximum as the configurable knob; a hardcoded plaintext constant; `recordSize` following the body limit |
| [012](0012-nullness-declared-with-jspecify.md) | Nullness declared with JSpecify | Accepted | Unmarked packages; a nullness contract that exists only in prose |
| [013](0013-release-and-publication-process.md) | Release and publication process | Accepted | A version constant in the build; a release implied by a merge; a namespace on a domain the project does not own |
| [014](0014-jpms-explicit-and-automatic-modules.md) | JPMS: explicit modules for the library, automatic for the starters | Accepted | A module or package name changed after release; a package split across two artifacts; a descriptor on a starter |
| [015](0015-vault-address-scheme-policy.md) | Vault address scheme: https, or loopback/opted-in http | Accepted | A non-HTTP scheme; a loopback decision made by name resolution; a configuration-only path to plaintext transport of the Vault token |
| [016](0016-endpoint-policy-is-a-required-decision.md) | The endpoint policy is a required decision | Accepted | A `PushSender` without an egress decision; an allowlist shipped by the library; a policy derived by resolving the endpoint; a configuration-only path to unrestricted egress under Spring |
| [017](0017-domain-rule-in-the-endpoint-allowlist.md) | A domain rule in the endpoint allowlist | Accepted | A public `EndpointPolicy` combinator; a rule kind contributed from outside the library; a domain rule matching a non-`https` scheme or a non-default port; a wildcard syntax inside an origin entry; a public-suffix judgement made by the library; a push service's zone shipped as a default |
| [018](0018-encoded-vapid-public-key-on-the-signer.md) | The encoded VAPID public key is part of the signer contract | Accepted; one clause superseded by [021](0021-retry-belongs-to-the-caller.md), another by [028](0028-the-test-kit-publishes-contracts-not-conveniences.md) | An abstract addition to `VapidSigner`; a general-purpose base64url codec published from the core; an encoder for the private scalar; a padded or configurable encoding; an on-curve check inside the `default` method, and a merely structural one inside the static |
| [019](0019-vapid-token-reused-until-it-nears-expiry.md) | The VAPID token is reused until it nears expiry | Accepted | A token cache behind an SPI; a shared cache level in front of the in-process one; an unbounded or unevictable cache; a proportional safety margin; a second spelling of "sign every time"; a signature taken under the cache's lock; an entry whose life a backwards wall-clock step can extend past its monotonic bound, or staleness judged finer than the second the wire carries; a cache invalidated by an authentication status; an entry filed under a key read separately from the one its header carries; a signature and a public key delivered by one SPI call; a token bounded by the signing key's life or naming more than one origin in `aud`; a `byte[]` cache key; a claim about a named push service accepting a reused token |
| [020](0020-subscription-endpoint-length-bound.md) | The subscription endpoint is bounded in length | Accepted | An endpoint above 2048 characters inside a `Subscription`; a size or weight bound inside the token cache; a configurable endpoint-length limit; a bound derived by name resolution; the endpoint, raw or redacted, inside the refusal's message |
| [021](0021-retry-belongs-to-the-caller.md) | Retry belongs to the caller | Accepted; one clause superseded by [027](0027-the-endpoint-policy-answers-with-a-value.md) | A retry loop inside the sender, in any configuration; a retry SPI, policy interface or any seam whose subject is retrying; a retrying wrapper shipped from this repository, in the core or in a module of its own; `RetryAfter.parse` as public API; a `Retry-After` clamped before it reaches the caller; an attempt count on an outcome the sender no longer controls; an operational outcome reported through the exception channel; a checked exception, which does not travel across `CompletableFuture`; a facade that catches every exception and launders a defect into a value; an unanswered POST reported as safe to repeat; an interrupted send reported as a retryable outcome; a retry advice enum, which is a retry policy renamed; a retryable class rule in place of a status matrix; an outcome named for delivery when the push service reported acceptance; a nested failure object forcing two dispatches for one decision; a capability URL disclosed by an outcome's components, its `toString()` or any other path a caller did not choose; a 5xx classified by its class alone, where the status's own specification has it answer about the request, about a configuration, or about the sending host's network rather than about this delivery; an unrecognised 5xx classified as permanent by omission from a positive list; an unavailable custodian's status withheld from the outcome that reports it |
| [022](0022-one-type-per-programmatic-action.md) | One exception type per programmatic action | Accepted; one clause superseded by [027](0027-the-endpoint-policy-answers-with-a-value.md) | A type per throw site; a type whose only consumer difference is the sentence it puts in a log; a type minted for who owns the fix, and equally a claim that this taxonomy expresses that ownership; a type for a configured value the boundary that accepted it should have refused; `IllegalArgumentException` as a base class for a type the library owns, or as the carrier of library semantics; a library exception not extending `RuntimeException` directly; message-text matching as the supported way to tell two conditions apart; a cancellation reported by `send` as the type of whatever it interrupted, and equally a seam obliged to recognise one; a cancellation delivered as a `CancellationException`, or an interrupted send reported by a future that answers `isCancelled()`; an interrupt-flag promise on the asynchronous path; a taxonomy settled one exception at a time in whichever ADR touches a failure next |
| [023](0023-one-size-limit-answerable-before-a-send.md) | One configured size limit, answerable before a send | Accepted | Two configurable size parameters, or any second knob derivable from the first; `recordSize` as public API in its role as a refusal threshold — a builder step, a Spring property, or a properties component retained so a removed key can be refused — a length-hiding feature configuring a padded body size rather than `rs`; data on the assessment's fitting branch, `WithinLimit` carrying no components and gaining them only by a breaking change; `rs` derived per message rather than per sender; a floor, default or historical constant applied to the derived `rs`; a degenerate `min` left in the pre-flight after its second operand became derivable; a hardcoded plaintext maximum, or a plaintext limit as the configured knob; a bare numeric budget on the public API, in any spelling that lets a caller reach the number without one guarded comparison first; a pre-flight answer typed as a `PushOutcome`, or an outcome variant reused to answer a question that sent nothing; a derived component published on an assessment because it shortens a log line; a pre-flight that replaces the refusal, or a `send` that trusts an earlier assessment; a tombstone over the removed property carried without an end, or an end written as a version number that does not exist yet |
| [024](0024-one-endpoint-policy-reachable-at-registration.md) | One endpoint policy, reachable where subscriptions are accepted | Accepted | An endpoint policy unreachable at the boundary where subscriptions are accepted, or withheld from a deployment that stated an allowlist and does not send; a second definition of the same rule, built by the application from properties another module owns; an accessor for the policy on `PushSender`; a second method on `EndpointPolicy` — a predicate, or an overload taking a `Subscription`; a core factory joining subscription parsing to the deployment's admission decision, or a library-owned registration boundary; a policy built inside an auto-configuration's sender factory and left unrepresented in the context; an application-supplied policy bean winning silently over a non-empty allowlist property, that conflict diagnostic reachable only through the sender, or the starter's own bean recognised by a name an application may equally choose, that name published as a constant, or the malformed-entry refusal left in the policy's factory method behind every broader refusal; a startup failure demanding an allowlist from a deployment that configured no sender; a claim that one rule spans processes, where the guarantee is one interpretation of the same properties and the values are configuration delivery; a registration check a later send trusts, or one offered as a replacement for the send's check; a boundary applying the policy to an endpoint the `Subscription` contract has not vetted first; a configuration-only path to unrestricted egress |
| [025](0025-delivery-is-off-by-statement.md) | Delivery is off by statement, never by omission | Accepted | A context that starts with the auto-configuration active, no sender and no statement that this deployment does not send; a warning as the whole answer to that state, or any shape in which `push2u.enabled=false` changes nothing a running process can observe; a three-valued activation switch, and a second property whose subject is how strictly the first is enforced; a `PushSender` that exists and declines to send; `push2u.enabled` reaching the endpoint policy, that policy declared where the switch's condition is applied, or a released version carrying the switch while the allowlist is still read only inside the sender's own auto-configuration, which carries the switch's condition; a Vault signer constructed, and its fetched mode's startup read performed, in a deployment that switched delivery off; a claim that a missing sender proves the deployment said no; a blank activating property read as a configured one, and equally that reading applied to the allowlist properties; one module's failure message naming another module's property prefixes or counting its keys, or a mechanism for one module to contribute its finding to another module's message; a partial-configuration diagnostic firing while a `VapidSigner` or `PushSender` bean exists, or in a deployment that switched delivery off; a startup check whose side of the switch is decided by where it was implemented rather than by what it is about; a general refusal outranking a starter's specific finding, an order taken from the registration sequence the framework promises only as far as it can, a test over that sequence offered as what pins it, a precedence declared on some of these checks and not the others, any check of the family without a position in the one list, or a refusal left inside an ordinary bean's factory method behind every post-processor; a check built on a mechanism that sorts into a different bucket or runs in an earlier phase — a registry post-processor, or one over the environment as it is prepared — so its declared number does not compare; a position declared where the framework will not read it, on the factory method or behind a method returning the post-processor interface; a check the table has running with delivery off, suppressed by anything the row does not mention; a table claiming to be exhaustive without saying which refusals need no row; the order pinned by a constant's value or by a test that sees only one module's checks; a tombstone without an end, or an end written as a version that does not exist yet; a starter's diagnostic sharing an auto-configuration with its contribution, and so deciding whether a signer exists before the local one is registered; a value of `push2u.enabled` that is neither `true` nor `false` read as either of them; a refusal an application's own missing-bean failure can precede; a signer starter whose contribution the refusal's condition is assumed to see although that starter declares no order; a startup failure pointing at an Actuator endpoint the failed context does not serve; a diagnostic assembled by reading the framework's condition report; a missing-bean analyzer answering "configure a signer" to a deployment that switched delivery off or to a context whose auto-configuration was excluded, or that analyzer's precedence over the framework's own left to the position a factories list gives it; an activation switch reaching the core |
| [026](0026-deferred-vault-key-fetch.md) | The Vault metadata read is deferrable; the advertised key still never moves | Accepted | A deferred mode that reads the key again after a successful read, retains anything but a successful pair, or consults Vault on a token-cache hit; a deferred mode selected by a step on a builder whose `build()` also reads Vault, or by a boolean property; `eager` ceasing to be the default; a deferred `build()` that contacts Vault or skips a check the eager one makes without reading a Vault response; a written `public-key-fetch` accepted beside a supplied `public-key`, an unrecognised value read as either mode, a blank value read as a written one, or a binding that cannot tell an unset property from a written one; a metadata read begun while the signer records another as active; a failed or cancelled read remembered as state; a fetching caller's cancellation delivered to a caller that was not interrupted, a waiter's cancellation ending a flight that serves others, or a cancellation shared because it was classified by the type it was wrapped in; a failure of neither contract type laundered into one of them; one exception instance thrown by several threads, an exception's declared values read once per observer rather than once per flight, or a promise about the concrete class of a reconstructed failure; a signature taken while the initialization guard is held; a claim that the health indicator makes the deferred mode fail-fast, or documentation leaving a reader expecting a healthy container from a check still pointed at the primary health group; any published operation that re-reads a key already read successfully — a `refresh()`, a TTL, a scheduled refresher; a key-version accessor published without a consumer; a rotation recipe omitting the routing of subscriptions created under the previous key |
| [027](0027-the-endpoint-policy-answers-with-a-value.md) | The endpoint policy answers with a value | Accepted | An endpoint policy that refuses by throwing; a second way to ask it — a retained `validate`, a predicate, an overload — derived or not, and equally the compatible mirror of that shape with `validate` abstract and `assess` derived from it, which keeps the exception thrown on every routine refusal and buys a delay it repays with a larger break later; a published exception type nothing throws, kept for compatibility, whose only use is a `catch` that never fires; the same method name kept across the change from `void` to a value, which leaves a call site that does not name the exception compiling unchanged and silently admitting every endpoint; a method name promising that the seam goes and touches what the endpoint names, when the ordinary implementation reads a `URI` and reaches nothing; a refused endpoint redacted by the implementation rather than by this library, and so any endpoint component on `Refused`; components on the permitting branch, now or later; a published singleton for that branch — a non-public canonical constructor, or an `INSTANCE` offering identity as something a caller may read; a `Refused` with the raw endpoint inside the reason it carries, and equally a `Refused` that refuses to be constructed, since a refusal thrown out of the seam is a defect and stops the fan-out this value exists to keep running; a reason a program branches on — a code, an enum or a second component — in place of the sentence an operator reads, this library writing no log lines itself; structured refusal data travelling through this library on a consumer's own subtype; a component on `Allowed` carrying an address a policy resolved; a `null` assessment or an exception out of the seam read as an operational outcome rather than as a defect; a change to where the policy is applied, to who owns the rule, or to how a refusal is classified once the sender holds it — only the channel the seam signals through moves |
| [028](0028-the-test-kit-publishes-contracts-not-conveniences.md) | The test kit publishes contracts, not conveniences | Accepted | A kit helper that exists because assembly is tedious rather than because the library owns the knowledge it carries; a `PushSender` fixture or factory in any spelling, and an unrestricted endpoint policy reached through one; a fixed VAPID pair or any key-shaped constant in a published artifact; a fixture selecting, naming or inspecting a JCE provider rather than taking the environment's, and any reference from the kit to either BouncyCastle flavour; an encoder taking key material a caller already holds and returning its private scalar as a string, in any module — the property ADR-018's clause protects, surviving its one-clause supersession by this record and reaching one artifact further; an accessor for the private scalar on `VapidKeys`; a published fake for `VapidSigner`; a second model of a browser subscription, or subscription components published separately so an incoherent set can be built from them; a fixture handing over the encrypted body, or a `toString()` rendering a capability URL, a header value or a token; a canned `PushOutcome` factory, a catalogue of the eight variants, and any second statement of the mapping from status to outcome; a rejected-input catalogue published without a kit test that keeps it true; the library's own fixtures — the receiver, the loopback certificate, the RFC vectors, the shared send-pipeline helper, the Vault fakes — published from any artifact; a second published artifact carrying the fixtures apart from the contract; a retry DSL, an assertion DSL, mutable configuration or a `reset` on the transport fake, per-endpoint routing being left out of the first version rather than ruled out; a mock push service listening on a socket, shipped as part of this decision; a contract test asserting that an `EndpointPolicy` answers deterministically |
| [029](0029-the-kit-states-what-an-endpoint-policy-owes.md) | The kit states what an endpoint policy owes | Accepted | A contract test that refuses to run against a policy which refuses nothing, and equally one reaching such a policy only through a second published type; a `default` implementation of the refusal witness, in any spelling letting a subject inherit "there is nothing to check here" without writing it; an optional, empty-able or otherwise skippable `allowedEndpoint()`, sought for symmetry with that witness, where the two extremes have different standing in this library and the contract follows the standing; a contract test asserting that an `EndpointPolicy` answers deterministically — ADR-028's entry executed rather than narrowed, so no assertion that one endpoint yields one variant, neither hidden in the concurrency check as a stable subtype nor produced by splitting the refusal check from the leak check into two observations of one endpoint; a concurrency check presented as proof of thread safety, or one built on the common `ForkJoinPool`; a leak check forbidding the origin or the fingerprint `Endpoints.redact` prints on purpose, and equally one searching only the raw spelling of a component a policy may have decoded, or only whole components, so that a reason naming one path segment or one query value — the whole of the bearer credential — passes it; a leak check run against a witness with nothing to leak, and so a witness requirement left to the implementor's judgement instead of asserted; a witness qualification rule admitting a marker that occurs, in either spelling the leak check searches, in the redaction a conforming policy may print, and a fixture's unfitness reported as a failure of the policy; a kit deriving its own probe endpoint by rewriting the one the implementor supplied; a contract check on which endpoints a policy admits or refuses, or on the wording of a refusal beyond what it must not contain; an endpoint reaching the contract that the seam's own precondition does not admit; a second redaction scheme in the kit, or any reach into what the core keeps package-private; a published `EndpointPolicy` fake, fixture or preconfigured subject |
| [030](0030-the-kit-states-what-a-transport-owes.md) | The kit states what a transport owes, and brings the server it needs | Accepted | A TLS harness published as supported fixture API, in any spelling — a public type, a public constructor, a `protected` accessor handing a subclass the running server, or a documented promise about its behaviour; the library's own fixtures published from any artifact, which is ADR-028's entry standing unchanged, and equally the two certificate builders consolidated by publishing either one; a TLS private key or certificate committed to this repository or shipped inside a published artifact; `com.sun.net.httpserver` reaching a published artifact; a contract obligation that the response body is never materialised, in any spelling asserting buffering did not happen, and a response-size check offered as one; a contract check for a timeout, for a retry policy or schedule, for a connection-pool property or for persistent-connection behaviour — the number of HTTP requests one `post` call produces being checked rather than ruled out, since the seam promises it; a request body compared by length rather than byte for byte; an unanswered request modelled by dropping the connection before or during the TLS handshake, which tests a transport that never sent anything; a lifecycle the kit invents for the transport — a teardown hook, a `close` call on the subject, an `AutoCloseable` expectation, or any other obligation `PushHttpClient` does not itself carry; a redirect target on the harness's own listener, which would turn a check about an origin the policy never assessed into a check about a path; a harness asserting the exact set of request headers, comparing header names case-sensitively, or rejecting a chunked request body; a harness listening on anything but loopback, or a certificate whose subject alternative name does not match the endpoint handed out; a contract admitting a plaintext endpoint, or a check falling back to one when TLS fails; trust material narrowed to one HTTP stack's type, or an endpoint handed over without the material needed to reach it; a scripted response sequence, an assertion DSL or mutable configuration on the harness; the deletion of this build's own redirect test before a kit self-test proves a redirect-following transport fails the contract's redirect check; a JCE provider named, selected or inspected anywhere in the harness |
| [031](0031-telemetry-is-emitted-by-the-deployment.md) | Telemetry is emitted by the deployment, and this library publishes what it means | Accepted | A published artifact carrying meters, spans, decorators or a telemetry facade for this library, under any name and on any abstraction — the one entry a later record may supersede, and only on the evidence this one names; a telemetry dependency in the core or in a starter, an optional or `compileOnly` one whose absence a condition tests included; a listener, observer, recorder or event seam on `PushSender`, in any spelling; a timer, counter, span or log line opened by this library's own code on the send path; an auto-configuration decorating a `VapidSigner`, `PushHttpClient` or `EndpointPolicy` bean the deployment did not itself wrap, and any property switching that on; a host-to-push-service mapping shipped as code from any module; the capability-bearing part of an endpoint — the URI, its path, a segment, its query, a value, its fragment — offered as a metric tag, a span attribute or a log field, at any cardinality, and a policy's refusal reason in any of those places; a raw origin or host published as a *low-cardinality* tag, or a backend-side cap offered as what makes one safe, the same origin on a trace attribute or a log line being the deployment's decision; a statement of the mapping from HTTP status to outcome anywhere but where `send` performs it; an accessor on `EndpointPolicy` for which endpoints it admits, added so a tag can be bounded by it; a promise, in either direction, about the thread-bound context a caller's instrumentation observes on the asynchronous path |
| [032](0032-starters-declare-a-minimum-spring-boot.md) | The starters declare a minimum Spring Boot, never impose one | Accepted | Spring Boot's BOM on `api` or `implementation` in either starter, or in any other configuration that is published; `spring-boot-autoconfigure`, or any other Spring Boot artifact, published from a starter without a version; `strictly`, `reject` or a version range on a Spring Boot dependency published from a starter, the first two discarded by the POM and enforced by Gradle so one ecosystem gets a hard failure the other never sees; a published requirement this library expects the consumer's build to *lower*; an upper bound on Spring Boot in any spelling, a closed range, a `reject` over future versions or a "tested up to" presented as a supported ceiling; a `springBoot` catalog value raised because a newer Spring Boot exists rather than because a starter needs an API the floor lacks or a published vulnerability anywhere in the graph the floor produces — Spring Boot's own artifacts or anything its BOM manages into that graph — is fixed by a patch of the same line, the second obligatory rather than discretionary; a floor move of either size made by editing that number in passing, a patch move narrowing the supported set as a line move does and both owed a `docs/MIGRATION.md` note, a line move additionally a record of its own; the two meanings split into two keys so the starters compile against one version and advertise another, a temporary substitution of the single key being distinguished by the version living in one place and no published artifact built with a substituted value; a starter compiled above its declared floor in the default build or in the build that publishes, by a platform on `compileClasspath` or any other path that raises it; a runtime or startup check of the framework's version, and a member of the startup-refusal list whose subject is which Spring Boot is present; a claim that the minimum is enforced for every consumer, where a Maven build and a Gradle build under `io.spring.dependency-management` overrule it; a claim that Spring Boot 3 cannot run these starters, where what was measured is that this build does not compile against the 3.5 BOM and a starter built against 4.1.1 starts a context on 3.5.9 with the health configuration backed off; Spring Boot's version manifest reaching a consumer as a live input to their resolution by any other route — a BOM published from this project, a `pom` variant, or a starter re-exporting managed versions under another name |

## An ADR is immutable once implemented

The record of what was decided, and when, is the whole artefact. So an ADR whose decision has been
implemented is not edited again — not to reword it, not to bring it up to date with the code, and
not to append an amendment.

**A decision that moves entirely gets a new ADR**, with the next free number, stating the new
decision and what it replaces. The superseded one keeps its number, its title and its body, and its
status line becomes:

```markdown
**Status:** Superseded by [ADR-NNN](NNNN-the-new-decision.md)
```

That line is the only edit its argument ever takes, beside the link repair below. A superseded ADR
is not deleted: its number is referenced from other ADRs, from `docs/DESIGN.md` and from the review
procedure, and a numbered decision that vanishes leaves those references dangling.

**A decision that moves only in part — one clause superseded while the rest of the ADR still
stands — takes the same one-line edit, in a form beside the full one rather than in place of it:**

```markdown
**Status:** Accepted; one clause superseded by [ADR-NNN](NNNN-the-new-decision.md)
```

and the index's status cell follows, in the index's own link spelling:
`Accepted; one clause superseded by [NNN](NNNN-the-new-decision.md)`. That line is likewise the
only edit the superseded ADR's argument takes — *which* clause it is stays out of it and lives in
the superseding ADR instead, because naming it in the old file's status line would put the new
decision's reasoning into a document that may not carry it. ADR-004 and ADR-019 are the worked
example: ADR-019 supersedes one sentence of ADR-004 and says which one; ADR-004's status line says
only that a clause was superseded and by what.

**That form accumulates.** An ADR can lose a second clause, and a third, to records that arrive
years apart, so the line extends rather than being replaced:

```markdown
**Status:** Accepted; one clause superseded by [ADR-NNN](NNNN-the-first.md), another by [ADR-MMM](MMMM-the-second.md)
```

and the index cell follows in its own spelling,
`Accepted; one clause superseded by [NNN](NNNN-the-first.md), another by [MMM](MMMM-the-second.md)`.
Each record is named in the order it arrived, so the line reads as the history it is. Rewriting the
line to name only the newest would erase a supersession that still stands, and there is no shorter
form: "two clauses superseded" without saying by what leaves a reader with nowhere to go, which is
the one thing the status line exists to prevent. Everything else holds unchanged — the superseded
ADR's argument is still untouched, and which clause each record took still lives in that record and
not here. [ADR-018](0018-encoded-vapid-public-key-on-the-signer.md) is the worked example: one
clause to [ADR-021](0021-retry-belongs-to-the-caller.md) and another to
[ADR-028](0028-the-test-kit-publishes-contracts-not-conveniences.md).

**One other edit is allowed, and only one: a link whose target document was renamed.** The same
document under a new name is the same artefact; the ADR's own text does not move, only the path
that reaches it. The reason this is worth an exception is that a rename can leave the old path
still *resolving*, at a document that is now something else — `docs/MIGRATION.md` became
[`MIGRATION-FROM-WEB-PUSH.md`](../MIGRATION-FROM-WEB-PUSH.md) while a new
[`MIGRATION.md`](../MIGRATION.md) took the freed name, so ADR-023's unedited reference would have
sent a reader to a document the parameter it names never appeared in. A dangling link announces
itself; that one does not.

**Material that moved is the opposite case, and takes no edit.** A fragment carried into another
document leaves the ADR's reference correct as the record of where that material was when the
decision was taken. ADR-026 is the worked example: it names [`VAULT.md`](../VAULT.md) as the home
of the key-rotation recipe, which has since moved to
[`VAPID-KEY-ROTATION.md`](../VAPID-KEY-ROTATION.md), and that reference stays exactly as it is.
Renamed artefact, fix the path; relocated material, leave it.

Before the decision is implemented, an ADR is still a draft and can be revised freely — the status
line says so (`Proposed`).

## Writing one

A new ADR earns its place when the decision constrains future changes. "We use `HttpClient`" is
not an ADR; "the transport is replaceable but the encryptor is not, because an alternative
encryptor would only introduce a silent wrong-ciphertext failure mode" is.

Match the house style: the heading `# ADR-NNN — Title`, a `**Status:**` line, then prose. State
the context as it was at the time, the decision, and what it rules out — including the
alternatives that were rejected and why, which is the part a later reader cannot reconstruct. Keep
the description of how things currently work in `docs/DESIGN.md`, where it can be updated; an ADR
that describes the present tense goes stale the moment the code moves, and it may not be edited to
catch up.

After adding one: the table above, and the cross-references that carry the range — `CLAUDE.md`,
`CONTRIBUTING.md` where it points contributors at the settled decisions, and `docs/DESIGN.md`
wherever the architecture it constrains is described.

Published sources are the one place an ADR is never cited: `push2u` ships a `sources.jar`, and a
consumer reading a comment there has no repository to follow the reference into. Code and Javadoc
carry the reasoning itself, in their own words — the build enforces this, see
[`CONTRIBUTING.md`](../../CONTRIBUTING.md#what-the-build-enforces).
