/**
 * Spring Boot auto-configuration for push2u — binds {@code push2u.*} to a ready
 * {@link io.push2u.PushSender} bean. Opt-in module on top of {@code push2u-core}.
 *
 * <p>Provisional package + Maven coordinate, finalised at extraction (DESIGN.md ADR-008/009).
 */
package io.push2u.spring;
