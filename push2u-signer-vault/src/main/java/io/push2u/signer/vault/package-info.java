/**
 * A {@link io.push2u.VapidSigner} backed by HashiCorp Vault Transit — the private key never leaves Vault. Opt-in module
 * on top of {@code push2u-core}.
 */
@NullMarked
package io.push2u.signer.vault;

import org.jspecify.annotations.NullMarked;
